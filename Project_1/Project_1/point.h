#ifndef POINT_H // guards
#define POINT_H
#include <vector>

using namespace std;
   
    // one line in the input file = one point
struct Point {
    vector<double> coords; // stores all coordinates of a point
};

#endif


